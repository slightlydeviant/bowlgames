{% extends "base.html" %}

{% block content %}
<div class="container">
	<h2>Leaderboard</h2>
	<table class="table table-bordered table-hover table-condensed">
		<tbody>
		{% for u in pointlist|slice:":9" %}
		{% if u.username == currentuser.username %}
		    <tr class="info">
		        <th>{{ forloop.counter }}</th>
		        <th>{{ u.first_name }}</th>
		        <td>{{ u.points  }}</td>
		    </tr>
		{% else %}
		    <tr>
    		    <th>{{ forloop.counter }}</th>
		        <th>{{ u.first_name }}</th>
		        <td>{{ u.points }}</td>
		    </tr>
		{% endif %}      
		{% endfor %}
		{% if currentuser not in pointlist|slice:":0" %}
		    <tr>
		        <th></th>
                <th></th>
                <td></td></tr>
            <tr>
                <th></th>
                <th>{{ currentuser.first_name }}</th>
                <td>{{ pts }}</td>
            </tr>
        {% endif %}
		</tbody>
    </table>
    
    
    
</div>
{% endblock %}
